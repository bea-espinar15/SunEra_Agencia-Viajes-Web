<!DOCTYPE html>
<html lang="es">

<head>
    <!-- Importar css -->
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/general.css">
</head>

<body>
    <!-- Tarjeta de un destino -->
    <div class="px-4 my-3">
        <div class="card p-0">
            <a href="/destino/<%= d.id %>">
                <!-- Imagen del destino -->
                <img src="/img/destinos/<%= d.pic %>" class="card-img-top" alt="Primera imagen del destino">
                <!-- Información del destino -->
                <div class="card-body">
                    <h3 class="card-title">
                        <%= d.name %>
                    </h3>
                    <p class="card-text">
                        <%= d.country %>
                    </p>
                    <div class="d-flex">
                        <img src="/img/rate/<%= d.ratePic %>" class="card-rate" alt="Valoración destino" height="20">
                        <p class="card-price">
                            <%= d.price %>€/p
                        </p>
                    </div>
                </div>
            </a>
        </div>
    </div>
</body>

</html>